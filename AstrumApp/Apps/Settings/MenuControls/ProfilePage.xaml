<UserControl x:Class="AstrumApp.Apps.Settings.ProfilePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AstrumApp.Apps.Settings"
             xmlns:services="clr-namespace:AstrumApp.Services"
             mc:Ignorable="d" 
             d:DesignHeight="585" d:DesignWidth="720">

    <Grid>
        <Border Width="600" Height="240" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,80,0,0" Padding="25"
                CornerRadius="22" BorderThickness="1" MouseLeftButtonDown="Border_MouseLeftButtonDown" 
                MouseLeftButtonUp="Border_MouseLeftButtonUp" Cursor="Hand">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF1B222A" Offset="0"/>
                    <GradientStop Color="#FF2A3642" Offset="0.5"/>
                    <GradientStop Color="#FF1F2A33" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>

            <Border.BorderBrush>
                <SolidColorBrush Color="#22FFFFFF"/>
            </Border.BorderBrush>

            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="0" Opacity="0.35"/>
            </Border.Effect>

            <Border.RenderTransform>
                <ScaleTransform/>
            </Border.RenderTransform>

            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                 To="1.03" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                 To="1.03" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                To="#4DA3FF"
                                                Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>

                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                 To="1" Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                 To="1" Duration="0:0:0.2"/>

                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                To="#22FFFFFF"
                                                Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Width="150" Height="150" CornerRadius="75" BorderBrush="#33FFFFFF" BorderThickness="1">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#2C3A46" Offset="0"/>
                            <GradientStop Color="#1B242C" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Image Source="{Binding Avatar}" Stretch="UniformToFill"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="25,0,0,0">
                    <TextBlock Text="{Binding Username}" FontSize="30" FontWeight="SemiBold" Foreground="White"/>

                    <TextBlock Text="{Binding Email}"  FontSize="14" Foreground="#BFD3E3" Margin="0,6,0,0"/>

                    <TextBlock Text="Registered: 01.01.2024" FontSize="12" Foreground="#88FFFFFF" Margin="0,10,0,0"/>

                    <Border Width="90" Height="24" CornerRadius="12" Background="#223A4B" Margin="0,12,0,0">
                        <TextBlock Text="Active" Foreground="#6EE7FF" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
