<Page x:Class="AstrumApp.Apps.Authentication.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AstrumApp.Apps.Authentication"
      mc:Ignorable="d" 
      xmlns:controls="clr-namespace:AstrumApp.Controls"
      d:DesignHeight="400" d:DesignWidth="300"
      Title="RegistrationPage">

    <Border Width="300" Height="400" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="5"
                CornerRadius="22" BorderThickness="1">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF1B222A" Offset="0"/>
                <GradientStop Color="#FF2A3642" Offset="0.5"/>
                <GradientStop Color="#FF1F2A33" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.BorderBrush>
            <SolidColorBrush Color="#22FFFFFF"/>
        </Border.BorderBrush>
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="0" Opacity="0.35"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
 
            <Path Width="20" Height="20" Stroke="#9FB3C8" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                  StrokeLineJoin="Round" Data="M 17,10 L 4,10 M 4,10 L 10,4 M 4,10 L 10,16" HorizontalAlignment="Left" 
                  VerticalAlignment="Top" MouseLeftButtonDown="Path_MouseLeftButtonDown" Margin="10" Cursor="Hand"/>

            <TextBlock Grid.Row="0" Text="Регистрация" Foreground="White" VerticalAlignment="Top" HorizontalAlignment="Center" 
                           Margin="0, 20, 0, 0" FontSize="32" FontWeight="Bold"/>

            <StackPanel Grid.Row="1" Margin="0, 30, 0, 0">
                <controls:InputField x:Name="EmailTextBox" Placeholder="Почта" Margin="20,0"/>
                <controls:InputField x:Name="PasswordBox" Placeholder="Пароль" IsPassword="True" ShowPasswordButton="True" 
                                         Margin="20,20,20,0"/>
            </StackPanel>

            <Button x:Name="NextButton" Content="Далее" Grid.Row="2" Width="160" Background="#4A8DFF" 
                        Foreground="White" BorderBrush="#35FFFFFF" Click="NextButton_Click"/>
        </Grid>
    </Border>
</Page>
