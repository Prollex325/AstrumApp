<UserControl x:Class="AstrumApp.Controls.RightPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AstrumApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="265" d:DesignWidth="210" Height="235" Width="210">

    <Grid Cursor="Hand">
        <Border CornerRadius="50" ClipToBounds="True" RenderTransformOrigin="0.5,0.5">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#704488F2" Offset="0"/>
                    <GradientStop Color="#7080D3ED" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>

            <Border.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Border.RenderTransform>

            <Border.Effect>
                <DropShadowEffect Color="#2F80FF" BlurRadius="10" ShadowDepth="0" Opacity="0.7"/>
            </Border.Effect>

            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.05" 
                                                         Duration="0:0:0.22">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>

                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.05"
                                                         Duration="0:0:0.22">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>

                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).BlurRadius"
                                                         To="13" Duration="0:0:0.22"/>

                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).Opacity" To="1"
                                                         Duration="0:0:0.22"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>

                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1"
                                                         Duration="0:0:0.22">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>

                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1"
                                                        Duration="0:0:0.22">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>

                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).BlurRadius" To="10" 
                                                         Duration="0:0:0.22"/>

                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).Opacity" To="0.7"
                                                         Duration="0:0:0.22"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>


                <TextBlock Grid.Row="0" Text="Погода" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>

                <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                    <TextBlock x:Name="WeatherDesc" FontSize="16" FontWeight="Bold" Foreground="White" TextAlignment="Center" TextWrapping="Wrap"
                               MaxWidth="200"/>

                    <TextBlock x:Name="WeatherCity" Text="г." FontSize="16" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="2" VerticalAlignment="Center">

                    <TextBlock x:Name="WeatherTemp" Text="• Температура: " FontSize="14" Margin="20,10,0,0" Foreground="White"/>

                    <TextBlock x:Name="WeatherFeels" Text="• Ощущается как: " FontSize="14" Margin="20,10,0,0" Foreground="White"/>

                    <TextBlock x:Name="WeatherHumidity" Text="• Влажность: " FontSize="14" Margin="20,10,0,0" Foreground="White"/>

                    <TextBlock x:Name="WeatherWind" Text="• Скорость ветра: " FontSize="14" Margin="20,10,0,0" Foreground="White"/>

                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
