<UserControl x:Class="AstrumApp.Controls.SecurityCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AstrumApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="270" d:DesignWidth="270">

    <UserControl.Resources>
        <Style x:Key="CardsStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="22"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderThickness" Value="1"/>

            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">

                        <GradientStop Color="#202B38" Offset="0"/>

                        <GradientStop Color="#1A2430" Offset="0.55"/>

                        <GradientStop Color="#161E27" Offset="1"/>

                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>

            <Setter Property="BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#30FFFFFF"/>
                        <GradientStop Color="#2A8EBBFF" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>

            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.15"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardsEllipseStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="150"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="0, -75, -75, 0"/>
            <Setter Property="Opacity" Value="0.12"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="Tag" Value="White"/>
        </Style>

        <Style x:Key="NavigationButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0, 1, 0, 0"/>
            <Setter Property="BorderBrush" Value="#30FFFFFF"/>
            <Setter Property="Padding" Value="20,14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="NormalBorder" Opacity="1" CornerRadius="0,0,8,8" BorderThickness="0,1,0,0" 
                                    BorderBrush="#22FFFFFF" Background="Transparent"/>

                            <Border x:Name="HoverBorder" Opacity="0" CornerRadius="8" BorderThickness="1" BorderBrush="#22FFFFFF"
                                    Background="#12FFFFFF"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="6, 0, 0, 0"/>

                                <TextBlock Grid.Column="1" Margin="0,0,18,0" VerticalAlignment="Center" Text="❯" FontSize="13" 
                                                       Foreground="#808080"/>
                            </Grid>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity"
                                                             To="1" Duration="0:0:0.2"/>

                                            <DoubleAnimation Storyboard.TargetName="NormalBorder" Storyboard.TargetProperty="Opacity"
                                                             To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>

                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity"
                                                             To="0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="NormalBorder" Storyboard.TargetProperty="Opacity"
                                                             To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="HoverBorder" Property="Background" Value="#20FFFFFF"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border MinWidth="200" MinHeight="200" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                Style="{StaticResource CardsStyle}" RenderTransformOrigin="0.5, 0.5">
            <Border.BorderBrush>
                <LinearGradientBrush x:Name="AnimatedBrush" StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#22FFFFFF" Offset="0"/>
                    <GradientStop Color="#4DA3FF" Offset="0.5"/>
                    <GradientStop Color="#22FFFFFF" Offset="1"/>

                    <LinearGradientBrush.RelativeTransform>
                        <TranslateTransform x:Name="BrushTransform" X="-1"/>
                    </LinearGradientBrush.RelativeTransform>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard x:Name="EnterHoverStoryboard">
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="BrushTransform" Storyboard.TargetProperty="X"
                                             To="1" Duration="0:0:0.7"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.03" Duration="0:0:0.3"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.03" Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard x:Name="LeaveHoverStoryboard">
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="BrushTransform" Storyboard.TargetProperty="X"
                                             To="-1" Duration="0:0:0.7"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.3"/>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
            <Border.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Border.RenderTransform>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="40"/>
                </Grid.RowDefinitions>
                <Ellipse Style="{StaticResource CardsEllipseStyle}" Grid.RowSpan="2">
                    <Ellipse.Fill>
                        <RadialGradientBrush>
                            <GradientStop Color="{Binding AccentColor, RelativeSource={RelativeSource AncestorType=local:SecurityCard}}"/>

                            <GradientStop Color="Transparent" Offset="1"/>
                        </RadialGradientBrush>
                    </Ellipse.Fill>
                </Ellipse>

                <Border Background="#10FFFFFF" CornerRadius="12" Padding="10,8" Grid.Row="0" >
                    <StackPanel Orientation="Horizontal">
                        <Border Width="45" Height="45" CornerRadius="21" VerticalAlignment="Top">
                            <Border.Background>
                                <SolidColorBrush Opacity="0.4" Color="{Binding AccentColor,
                                                                       RelativeSource={RelativeSource AncestorType=local:SecurityCard}}"/>
                            </Border.Background>
                            <Image Source="{Binding Icon, RelativeSource={RelativeSource AncestorType=local:SecurityCard}}" Width="30" 
                                   Height="30" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=local:SecurityCard}}" 
                                   VerticalAlignment="Center" Foreground="#F2F5F8" FontSize="17" FontWeight="SemiBold" 
                                   Margin="10, 0, 0, 0"/>
                    </StackPanel>
                </Border>

                <ContentPresenter Content="{Binding CardContent, RelativeSource={RelativeSource AncestorType=local:SecurityCard}}"
                                  Grid.Row="1" Margin="0,15,0,0"/>

                <Button Content="{Binding NavButton, RelativeSource={RelativeSource AncestorType=local:SecurityCard}}" 
                        Grid.Row="3" Style="{StaticResource NavigationButtonStyle}" Click="Card_Click"/>

            </Grid>
        </Border>
    </Grid>
</UserControl>
