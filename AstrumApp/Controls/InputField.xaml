<UserControl x:Class="AstrumApp.Controls.InputField"
             x:Name="root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AstrumApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="90" d:DesignWidth="248">

    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBlock Text="{Binding Placeholder, ElementName=root}" FontSize="14" FontWeight="SemiBold" Foreground="#9FB3C8" 
                           Margin="2,0,0,0" Grid.Row="0" x:Name="PlaceholderTextBlock">
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation Storyboard.TargetProperty="Foreground.Color" 
                                                To="#B3C7DA" Duration="0:0:0.15"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>

                        <EventTrigger RoutedEvent="MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation Storyboard.TargetProperty="Foreground.Color" 
                                                    To="#9FB3C8" Duration="0:0:0.15"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">

                            <Setter Property="Visibility" Value="Visible"/>

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Placeholder, ElementName=root}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>

                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <PasswordBox x:Name="HidePasswordBox" MaxLength="{Binding MaxLength, ElementName=root}" 
                             Style="{StaticResource DefaultPasswordBox}" PasswordChanged="HidePasswordBox_PasswordChanged"/>

                <TextBox x:Name="VisiblePasswordBox" MaxLength="{Binding MaxLength, ElementName=root}" 
                         TextChanged="VisiblePasswordBox_TextChanged" Style="{StaticResource DefaultTextBox}"
                         AcceptsReturn="{Binding AcceptsReturn, ElementName=root}"
                         TextWrapping="{Binding TextWrapping, ElementName=root}"
                         VerticalScrollBarVisibility="{Binding VerticalScrollBarVisibility, ElementName=root}"
                         Height="{Binding Height, ElementName=root}"
                         Text="{Binding TextContent,
                   ElementName=root,
                   Mode=TwoWay,
                   UpdateSourceTrigger=PropertyChanged}" >

                    <TextBox.Resources>
                        <Style TargetType="ScrollBar">
                            <Setter Property="Width" Value="6"/>
                            <Setter Property="Background" Value="Transparent"/>

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ScrollBar">
                                        <Grid Background="Transparent">

                                            <Track x:Name="PART_Track" IsDirectionReversed="True">

                                                <Track.Thumb>
                                                    <Thumb>
                                                        <Thumb.Template>
                                                            <ControlTemplate TargetType="Thumb">
                                                                <Border x:Name="ThumbBorder" Background="#4A5663" CornerRadius="6"
                                                                        Margin="0, 2, 3, 2"/>

                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="ThumbBorder" Property="Background" 
                                                                                Value="#647487"/>
                                                                    </Trigger>

                                                                    <Trigger Property="IsDragging" Value="True">
                                                                        <Setter TargetName="ThumbBorder" Property="Background"
                                                                                Value="#7B8DA1"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Thumb.Template>
                                                    </Thumb>
                                                </Track.Thumb>

                                                <Track.DecreaseRepeatButton>
                                                    <RepeatButton Command="ScrollBar.PageUpCommand" Background="Transparent"
                                                                  BorderThickness="0" Focusable="False">
                                                        <RepeatButton.Template>
                                                            <ControlTemplate TargetType="RepeatButton">
                                                                <Border Background="Transparent"/>
                                                            </ControlTemplate>
                                                        </RepeatButton.Template>
                                                    </RepeatButton>
                                                </Track.DecreaseRepeatButton>

                                                <Track.IncreaseRepeatButton>
                                                    <RepeatButton Command="ScrollBar.PageDownCommand" Background="Transparent"
                                                                  BorderThickness="0" Focusable="False">
                                                        <RepeatButton.Template>
                                                            <ControlTemplate TargetType="RepeatButton">
                                                                <Border Background="Transparent"/>
                                                            </ControlTemplate>
                                                        </RepeatButton.Template>
                                                    </RepeatButton>
                                                </Track.IncreaseRepeatButton>
                                            </Track>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                    </TextBox.Resources>
                </TextBox>

                <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#9FB3C8" Margin="0,25,6,10" Grid.Row="0" 
                           x:Name="CounterTextBlock" Visibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Bottom" Text="10"/>

                <ToggleButton x:Name="PasswordButton" Background="Transparent" BorderThickness="0" Foreground="White" Content="👁️"
                              Width="20" Height="20" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,25,6,0"
                              Click="PasswordButton_Click" Visibility="Collapsed">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid>
                                <Border x:Name="ButtonBorder"  Background="Transparent" BorderThickness="0"
                                     Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <Path x:Name="Slash" Stroke="White" StrokeThickness="2" Data="M 4,4 L 16,16" Visibility="Collapsed"/>

                            </Grid>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="Transparent"/>
                                    <Setter TargetName="Slash" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="Transparent"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
            </Grid>

            <TextBlock x:Name="InfoText" Text="" VerticalAlignment="Bottom" FontSize="10" FontWeight="SemiBold"
                       Foreground="White" Margin="5, 0, 0, 0" Grid.Row="1" Visibility="Visible" Opacity="0"/>
        </Grid>
    </Grid>
</UserControl>
